# Database Rules

Use UUID.

Use Foreign Keys.

Use Indexes.

Normalize tables.

Use created_at.

Use updated_at.

Use deleted_at.

Never duplicate data.

Always create Alembic migrations.

Never modify production database manually.
